# osc-bridge

Per-room OSC delivery service for Sentient → SCS (UDP + app-level acks/retries).

Current status: placeholder skeleton (starts, logs config, idles). OSC contract and MQTT integration will be implemented next.

