# Rooms

Each room is deployed as an independent Docker Compose project (core + broker + DB + osc-bridge).

Use `scripts/new-room.sh` to create a new room instance from `infra/compose/room-template/`.

